<h2>v0.0.0-beta.9 - Gem Loot Modifier Syntax Fix</h2>

<h3>Fix</h3>
<ul>
<li><strong>Gem Loot Modifiers</strong>: Corrected NeoForge 26.2 entity predicate syntax in <code>gem_entity_drops.json</code> and <code>gem_entity_drops_from_real_players.json</code>. Changed invalid <code>type_specific</code> wrapper to <code>sub_predicate</code> to properly reference the <code>is_monster</code> entity sub-predicate. The old Apotheosis 26.1.2 syntax was incompatible with NeoForge 26.2 predicate parsing.</li>
</ul>

<h3>Technical Changes</h3>
<ul>
<li>Loot modifiers now correctly parse without data file errors on server startup.</li>
<li>Gem drops from monsters will now function as intended.</li>
</ul>

<blockquote>
<strong>Note:</strong> This release resolves parsing errors that prevented gem drop mechanics from functioning. All players should update to ensure proper loot dropping from mobs.
</blockquote>
